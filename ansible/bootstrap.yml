---
- name: Install mark2
  hosts: mark2
  remote_user: luke
  roles:
    - mark2
  tags:
    - mark2

- name: Install java
  hosts: java
  remote_user: luke
  roles:
    - java
  tags:
    - java

- name: Setup initial basic security practices
  hosts: security-init
  remote_user: luke
  roles:
    - security-init
  tags:
    - security-init

- name: Common setup files (motd, ssh key, etc)
  hosts: common
  remote_user: luke
  roles:
    - common
  tags:
    - common
    - motd

- name: System upgrades
  hosts: upgrades
  vars:
  - do-full-upgrade: true
  remote_user: luke
  roles: 
    - upgrades
  tags:
    - full
    - upgrades
